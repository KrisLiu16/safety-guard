# 训练完成验收清单

目标：把本轮自己的流式风险分类模型端到端训练完。所有神经网络训练、推理及正式速度测试在 L20；生成数据的既有 v12 格式不变。以下项目须以实际产物核对，计划文件、提交成功或进程存在均不能代替完成证据。

| 要求 | 完成证据 | 当前状态 |
|---|---|---|
| 数据来源、标签权重、格式及划分可复查 | `data/risk_v2/manifest.json`、四个 JSONL 的 SHA；原 Aster v12 产物及独立下游视图 | 冻结完成；已复核跨划分 token-ID 精确无交集 |
| 完整/窗口/可学习记忆三个分类器实际训练 | 三份 `sft_summary.json`、4164 次更新日志、检查点 SHA、导出后开发指标 | full、window、memory 均已完成各 4164 次更新 |
| 保留所有 24 层、不通过删层假装结构优化 | 实际网络及 setup 参数量；6 个注意力层窗口/记忆实现，18 个 GDN 层保留 | 实际结构、参数数量和最终权重审计通过；全部 24 层保留 |
| 分类 RL 实际执行、不生成文本 token | `classification_rl/summary.json`、256 次更新轨迹、reward/FP/FN/KL 和 SFT 对照 | 256 次更新完成，开发集选择 step 192，完整轨迹已收集 |
| 以风险而非教师相似度衡量能力 | 独立校准/开发/保留测试指标及逐例预测；A0 同集参考 | 开发选择固定，保留测试及 A0 已完成；结果没有全面胜过 A0 |
| 真实导出权重支持长 token-ID 流式分类 | `long_stream_audit.json`：四份检查点 SHA 对齐、513/1025/4097/8192 长度、双角色、不同分块、窗口状态有界 | full/window/RL 通过；memory 最大误差 .032561 > .03，被排除且不放宽容差 |
| 可运行整段/分块分类入口 | `inference_cli_audit.json`、逐块 JSON 输出及整段/分块概率一致性 | 整段/分块双角色 L20 CLI 实测通过 |
| 接收实际文本片段并处理 BPE 尾部重分词 | 文本会话状态机、L20 任意文字切分/回滚/跨角色/会话隔离实测，失败事务不污染状态 | 四个分支均通过实际文本流检查；加速文本接口正在独立 L20 验证 |
| 用户指定 Qwen3GuardTest 观测 | 三个完整分片覆盖数及严格/宽松判决、A0 对照；明确原生分词适配范围，不冒用原 token 定位指标 | 两边各 2441 条完整评测完成；主候选 thinking 流式 FPR 90.41%，构成质量阻碍 |
| 含词但无风险、同词不同意图与安全拒绝诊断 | 256 个四划分之外的新词族、1024 条明确模板测试；最终候选/A0 使用原有阈值；缺少的 en/user 校准单独说明 | A0 完成；候选因分词资产差异停止，1024 条实际 modern ID 证明通过后独立补测 |
| 用 ITPS、分类延迟和内存衡量效率 | 同硬件、同 context/chunk 的 L20 结果；最终候选/A0 各六组、每组 128 次同协议追加；实际文本流另报分词/回放成本 | 实际文本流及 window/chunk8 graph 已测；A0 首版长度越界，双方 v2 同场补测正在运行 |
| 可复现且可加载的最终研究检查点 | `MODEL_MANIFEST.json`、PVC 上实际权重 SHA、`INFERENCE_USAGE.md`、运行代码/数据版本 | 主清单固定 RL/full 29fc3e...；独立包验收已通过，完整本地权重下载中 |
| 产物保存，专属 GPU 任务退出 | 本地完整模型包及所有文件 SHA、报告和预测；PVC 权重；相关 Job 终态与节点占用 | 主训练/基线验证 GPU Job 已退出；快速文本验证使用同一 L20 串行执行 |

本轮硬标签覆盖 safe/unsafe。保留的 controversial 输出和类别头不作为本轮已训练验证的能力；后续政策细化、第三档标注、MoE、量化、连续批处理以及有证据位置奖励的 allow/hold/block 控制属于另列的后续研究，不能写成已完成。训练完成也不等于在未知政策和负载下获得生产效果保证。速度或能力若无提升，要按实测报告，不能用完成训练代替效果结论。

新任务只用于训练后的实际文本流及完整公开基准验证，沿用同一 L20/PVC，等待主训练 Job 完成后再申请，不与训练并行抢卡、不修改他人负载。

第五轮正在准备针对前缀的独立后训练。整段分数不能替代流式误报和召回；新训练数据保留原样本格式，未知危险起点不填硬标签，官方已看过的测试仅作回归观察。当前目标尚未完成。
