# 第四轮执行 DAG：直接优化风险识别

日期：2026-09-23。当前实际状态见 [EXECUTION_STATUS.json](EXECUTION_STATUS.json)；完成以结果文件为准，不把提交或梯度检查当作质量提升。

```mermaid
flowchart TD
    A[20,000 条公开风险参考] --> D[冻结 33,311 条训练记录]
    B[Aster 912 分片已取回\n488,560 条格式合格] --> C[选择同词安全/风险弱标签对\n8,192 条，降权]
    C --> D
    E[含词但无风险的明确 rubric\n4,096 条，含助手安全拒绝] --> D
    F[1,023 条弱监督长背景样本] --> D
    D --> G[full 完整主干\n风险交叉熵，已完成 4,164 次更新]
    D --> H[window 渐进窗口\n2048→1024→512，已完成 4,164 次更新]
    D --> I[memory 窗口加可学习历史记忆\n约新增 98,352 参数，已完成 4,164 次更新]
    J[L20 记忆核、因果、流式状态、组合反传\n已通过] --> I
    G --> K[独立校准与开发风险指标\n不按教师相似度选模]
    H --> K
    I --> K
    K --> L[full 候选分类 RL，256 步已完成\n正确 +1，误拦 -3，漏报 -4]
    K --> M[保留 SFT 检查点作对照]
    L --> N[固定模型选择后评测保留测试集]
    M --> N
    O[A0 原版安审模型同集参考] --> N
    N --> P[L20 ITPS、分类延迟、状态空间\n与风险质量一起报告]
    P --> R[实际导出权重 8K 长流检查\n整段/分块一致性及缓存上界]
    R --> Q[收集主训练报告，释放首个 GPU Job\n长流检查失败的候选不能晋升]
    Q --> S[固定开发集候选与验证输入清单]
    S --> T[同一 L20 串行验证\n实际文本流、重分词回滚、会话隔离]
    T --> U[完整 Qwen3GuardTest 与同词不同意图诊断\n候选 / A0 使用既定判决规则]
    U --> V[完整权重独立加载验收\n整段与流式分类，不依赖旧训练目录]
    V --> W[收集验证证据，释放 GPU]
    W --> X[CPU-only 下载模型包\n逐文件 SHA 验证]
    U --> Y[发现前缀误报过高\n端点监督不足以支撑流式判断]
    Y --> Z[设计独立前缀后训练与流式校准\n不得用官方测试样本训练]
    W --> AA[window CUDA Graph 实际文本接口\n固定权重独立验证]
```

三组各训练 2 个 epoch，完整数据顺序和预算一致。校准集 900、开发集 1,200、保留测试 1,200；以真实风险标签上的召回、误报、PR-AUC 等衡量能力，保留每次验证曲线，不因一次小实验下降就否定路线。

分类 RL 采样的是风险判断动作，不是文本 token。其奖励来自 [rubric](rubric.json)与样本标签；生成弱标签降权，阶段内的参考策略 KL 仅限制更新幅度，不充当风险真值。此轮尚非 `allow/hold/block` 流式释放策略，也不声称生产验收完成。
