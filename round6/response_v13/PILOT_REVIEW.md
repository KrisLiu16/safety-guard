# v13 pilot 复核（aster-dev-274）

2026-09-24。共 50 个词、27 个 Task，模型 gpt-5.6-luna，并发 27，最多尝试 1 次。原始统计见 [pilot/extracted/summary.json](pilot/extracted/summary.json)，逐条内容见 `pilot/extracted/review.txt`。第一次提交 aster-dev-273 只打包了 `flow.py`、漏了 `pipeline.py`，启动即失败，0 次模型调用，见 `pilot/run_failed_aster-dev-273.json`。

## 数字

- 4 条都合格的词：44/50（88%）。合格回答 192/200，按格分别为 50 / 47 / 49 / 46。
- luna 显式拒答 0 次，包括性侵、毒品类提问。
- 格式失败共 13 次：起点引用对不上 6 次，unsafe 格填了 benign 4 次，其余 3 次。
- 起点位置（占回答长度的比例）：立即型 22/24 在第 0 个字；延迟型中位数 0.39；跑偏型中位数 0.52。
- 每词约 2,878 输入 + 753 输出 token，单词耗时中位数 56 秒。按此估算，5 万词约 1.44 亿输入 + 0.38 亿输出 token。

## 人工逐条复核 93 条 unsafe 回答

| 问题 | 条数 | 处理 |
|---|---:|---|
| 实际是安全的：写的是“不要/避免这样做”，起点片段把否定词切在了外面 | 5（都在跑偏格，占 46 条中的约 11%） | v14 由程序拒收：起点前 12 字内出现否定词即判 `negated_onset`。回放 v13 数据，恰好拦下这 5 条，没有误伤 |
| 起点后接一句“这种做法会造成伤害”之类评论 | 22 | 可能教出“出现危害评论就判 unsafe”的捷径（safe 回答中只有 7/99 条出现）。v14 提示词禁止，程序只打标记、不拒收 |
| 用“虚构/fictional”标注编造细节 | 25（safe 回答中 13/99 条） | 同样是捷径。v14 提示词禁止，程序打标记 |
| 描述性写法、半带免责（如“A post could falsely claim …”） | 约 6 | 标签偏模糊。v14 的“不评价危害”规则应能减少 |

safe 回答质量好：拒答、引导、思考体风险分析、中性科普都自然，并且会描述用户的有害意图，正是官方 thinking 集里被误报的那类难负例。回答的开头几个字没有明显泄露标签。

## 结论

2×2 配对和起点引用的格式可行，luna 愿意写危险回答。需要修的是标签纯度，已做成 v14（[../response_v14](../response_v14)）。建议先用 50 个新词再跑一次 pilot，确认问题下降后再放大到 5 万词。
