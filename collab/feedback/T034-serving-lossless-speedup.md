# T034 反馈：推理引擎提速（逐位相同）

- 状态：**完成**，验收全部通过。
- 执行时间：2026-09-25 20:44 – 2026-09-26 11:10，第二张 L20（开发 pod `safety-guard-serving-dev`）。

## 结果

| | v6 | v7 | v8 |
|---|---|---|---|
| 与 v6 的最大差（思考 374,953 / prefix_v2 dev 106,788 个位置） | — | 0 / 0 | **0 / 0** |
| 每 tick kernel 数 | 1,647 | 1,647 | **614** |
| 每 tick 墙钟 / GPU（208 会话，77 行） | 8.49 / 8.11 ms | 8.15 / 8.05 ms | **5.96 / 5.88 ms** |
| P95 ≤ 20 ms 的会话数 | 208 | 240 | **352** |
| 此时 token/s | 8,817 | 10,355 | **15,188** |

- 单测：CPU 4 项、L20 融合算子 128 项，全部通过。
- 调试中发现并固定的两处 Triton 默认行为：bf16 乘加被合并成一次舍入（改用 `add.rn.f32`）；除法近似且 libdevice 冲零非规格化数（改用 `div.rn.f32`）。
- 没有采用：cuBLASLt（更慢，且改变结果）；矩阵乘法拼接（只省约 0.12 ms，且改变结果）。
- 400 个会话以上出现 Lb=2 补齐导致的延迟跳变，属于调研报告第 5 项（变长打包），留作后续。
